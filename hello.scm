(define (main args)
  (print "Hello, world.")
  0)
;; これは行コメントの例
(print "Hello world!")                  ;asa

#|ブロックコメントの例
#|
ブロックコメントはネストできるので，すでにコメントアウト
されているブロックをそのままコメントに含められます．
|#
|#

#;(begin (display "Hello, ")
         (display "world"))

